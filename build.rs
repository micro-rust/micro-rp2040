//! `micro-rp2040` build script.
//! Places the linker file in a linker accessible location.


use std::env;
use std::fs::{ File, OpenOptions };
use std::io::{ Read, Write };
use std::path::{ PathBuf };


fn main() {
	
}
